{
  "lexicon": 1,
  "id": "space.roomy.message.getMessage",
  "defs": {
    "main": {
      "type": "query",
      "description": "Returns a single message by ID. The message's room is resolved first, then read access on that room is enforced. Returns NotFound if the message does not exist.",
      "parameters": {
        "type": "params",
        "required": ["messageId"],
        "properties": {
          "messageId": { "type": "string" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "space.roomy.room.getMessages#message"
        }
      },
      "errors": [
        { "name": "NotFound", "description": "The requested message does not exist." }
      ]
    }
  }
}
