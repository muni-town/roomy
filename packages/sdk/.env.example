# ATProto Test Account Configuration
ATPROTO_TEST_DID=did:plc:...
ATPROTO_TEST_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Leaf Server Configuration
# For local development with Docker: use http://leaf-server:5530
# For testing against public Leaf: use https://leaf.muni.town
LEAF_URL=http://leaf-server:5530

# Leaf Server DID (computed from LEAF_URL hostname for production)
# For local development, use did:web:localhost regardless of LEAF_URL
LEAF_DID=did:web:localhost

# Optional: Use existing test space instead of creating a new one
# ROOMY_TEST_SPACE_ID=did:web:existing.space
